<h1>Студент </h1>
<%= button_to "Добавить нового студента", new_student_path, method: :get %>
<ul>
	<% @students.each do |student| %>
	<li><%= link_to student.inputsCertificate, student_path(student) %></li>
	<% end %>
</ul>